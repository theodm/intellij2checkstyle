package de.theodm.intellij2checkstyle

import org.gradle.api.Plugin
import org.gradle.api.Project

/**
 * Empty gradle Plugin.
 */
open class I2CPlugin : Plugin<Project> {
    /**
     * Default apply function.
     */
    override fun apply(target: Project) {
    }
}
